<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <%= f.label :users %><br>
    <%= select_tag("account[user_ids]", options_for_select(@user.all.collect {|t| [t.name, t.id]}, @account.user_ids), {:multiple => true, :prompt => 'Select Users For Account', :class => "form-control" } ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
